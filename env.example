# Public environment variables (safe to expose to the browser)
# Do NOT include secrets here. Copy to .env.local and adjust per environment.

# Base API for fetch-based endpoints (defaults to production URL if unset)
NEXT_PUBLIC_API_BASE=https://ecom.nearbydoctors.in

# Base API for axios-based auth/login and related endpoints
NEXT_PUBLIC_API_BASE_URL=https://api.9nutz.com/api

# Optional alternative production API base used in older code paths
PROD_API_URL=https://9nutsapi.nearbydoctors.in/public/api/

# Example local override (uncomment and update for local testing)
# NEXT_PUBLIC_API_BASE=http://localhost:3001
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
